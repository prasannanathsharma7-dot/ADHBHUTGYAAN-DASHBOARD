# Copy to dashboard/.env.local (Next.js only reads env files from inside
# the app root when you run `npm run dev` from within dashboard/).

MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority
MONGO_DB_NAME=astrology_intelligence
